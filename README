ElpAPI version 0.5 (beta 1)

ElpAPI is the python API for developing software with the Elphel cameras, 
there are two kinds of scripts with this API:

the API meta scripts:
which are the scripts that conform the API

and the scripts:
which are the scripts that use the API

in this convention autosetup.py is a script that uses ElpAPI in order to 
configure the camera.

check autosetup.py and specially the core ElpAPI.py to know how to work with it.

all the code is GPLv3
